Based on the current log, a ping flooding attack is not happening.

Explanation:  
The ICMP type field in the Zeek conn.log snippet shows multiple entries with type 8, which corresponds to echo request (ping) packets. However, the volume and pattern of these packets do not indicate a flood. The timestamps are very close but the number of packets per source IP is low (mostly 1 per entry), and there is no evidence of rapid repeated ICMP echo requests from a single source to a single destination that would characterize a ping flood.

No anomaly records or heuristic context are provided, so no explicit heuristic or taxonomy can be referenced. Generally, a ping flood heuristic would involve detecting a high rate of ICMP type 8 packets from one or multiple sources targeting a victim IP, which is not observed here.

Next steps:  
- Monitor for increased frequency or volume of ICMP echo requests from these or other IPs.  
- Investigate if any of these IPs have a history of suspicious activity.  
- Correlate with other logs or IDS alerts for potential reconnaissance or other ICMP-based attacks.