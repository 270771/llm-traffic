Based on the current log, a ping flooding attack is not happening.

Explanation:  
- The ICMP type field in the Zeek conn.log snippet is indicated by the ICMP code field (the 11th field in Zeek conn.log is usually the ICMP type or code). Here, the ICMP type values are 2 and 0, not 8 (Echo Request).  
- There are only two ICMP entries, which is far below the threshold of at least 10 ICMP type 8 packets required to consider a ping flood.  
- No anomaly records or heuristic context are provided to suggest known suspicious IPs or other attack types.

No other attack type is clearly indicated by the given data. The ICMP types present (2 and 0) correspond to different ICMP messages (e.g., type 2 = "Destination Unreachable - Protocol Unreachable", type 0 = "Echo Reply"), which do not match ping flood patterns.

Next steps:  
- Investigate the nature of these ICMP messages further to ensure they are not part of a reconnaissance or other network issue.  
- Monitor for increased ICMP Echo Request (type 8) traffic to detect potential future ping floods.  
- Collect more anomaly data and heuristic context to improve detection accuracy.