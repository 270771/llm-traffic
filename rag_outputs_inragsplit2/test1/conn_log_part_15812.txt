Based on the current log, a ping flooding attack is not happening.

Explanation:  
- The only ICMP entry with type 8 (echo request) appears once (third log line: src IP 52.236.221.123, dst IP 163.252.70.66).  
- The other ICMP entries have types 11 and 1, which are not echo requests and thus not ping floods.  
- There are fewer than 10 instances of ICMP type 8, and no anomaly records or heuristic context are provided to override this threshold.

No other attack patterns are clearly indicated by the given data. Since no anomaly or heuristic context is available, no taxonomy class can be assigned.

Next steps:  
- Monitor for increased frequency of ICMP type 8 packets from the same source IPs.  
- Collect more comprehensive logs to detect potential low-rate or stealthy ICMP-based attacks.  
- Investigate the source IP 52.236.221.123 if it repeatedly sends echo requests in a short time frame.