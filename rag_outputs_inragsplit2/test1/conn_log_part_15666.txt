Based on the current log, a ping flooding attack is not happening.

Explanation:  
- The ICMP type in both entries is 8, which corresponds to echo request (ping), so the traffic is ICMP ping.  
- However, there are only 2 instances of ICMP type 8 in the snippet, which is below the threshold of at least 10 instances required to consider it a ping flood attack.  
- No anomaly records or heuristic context are provided to suggest known suspicious IPs or exceptions (heuristic 20) that would lower the threshold.  

No other attack patterns are evident from the given data. Without additional anomalies or heuristic matches, this traffic appears to be normal ICMP echo requests.

Next steps:  
- Continue monitoring for increased frequency of ICMP type 8 packets from these or other IPs.  
- Investigate if these IPs have a history of suspicious activity in other logs or anomaly records.  
- If more data shows a surge in ICMP echo requests (≥10), re-evaluate for ping flood.