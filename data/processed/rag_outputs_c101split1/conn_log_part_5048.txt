Based on the current log, a ping flooding attack is not happening.

Explanation:  
All entries in the Zeek conn.log snippet show TCP traffic (protocol field = tcp) with no indication of ICMP traffic. Since ping flood attacks rely on ICMP echo request packets (ICMP type 8), and no ICMP traffic is present, this cannot be a ping flood. Additionally, no anomaly records or heuristic context are provided to suggest other attack types.

Heuristic and Taxonomy:  
No heuristics or taxonomy classes are available from the provided data. However, the absence of ICMP traffic and the presence of only TCP connection attempts (mostly SYN packets with S0 state) suggest possible scanning or connection attempts but not a ping flood.

Next Steps:  
Investigate the high number of TCP connection attempts with S0 state (SYN sent, no response) as this may indicate a TCP SYN scan or reconnaissance activity. Correlate with other logs or IDS alerts to confirm. Also, monitor for any ICMP traffic to detect potential ping floods in the future.