Based on the current log, a ping flooding attack is not happening.

Explanation:  
- The ICMP types observed are 0 and 8 (from the snippet, one entry shows ICMP type 8, the other type 0). However, only one instance of ICMP type 8 is present, which is below the threshold of at least 10 instances required to indicate a ping flood.  
- No anomaly records or heuristic context are provided to suggest known suspicious IPs or heuristic 20 matches that would override the instance count rule.  
- The traffic does not match the pattern of repeated ICMP echo requests (type 8) in volume or frequency indicative of a ping flood.

No other attack type is clearly indicated by the given data. The heuristic and taxonomy context is unavailable, so no alternative classification can be assigned.

Next steps:  
- Monitor for increased frequency of ICMP type 8 packets from these or other IPs.  
- Collect more extended logs to verify if a pattern of repeated ICMP echo requests emerges.  
- Investigate the source IPs for any other suspicious activity or alerts in other logs.