Based on the current log, a ping flooding attack is not happening.

Explanation:  
- All ICMP entries have type 8, which is consistent with ping requests.  
- However, there are only 6 instances of ICMP type 8 in the snippet, which is below the threshold of 10 required to confirm a ping flood attack unless known anomalous IPs matching heuristic 20 are present.  
- No anomaly records or heuristic context are provided to suggest these IPs are known threats or match heuristic 20.  

No other attack type is clearly indicated by the data. The traffic appears to be normal or low-volume ICMP echo requests.

Next steps:  
- Monitor for an increase in ICMP type 8 packet frequency to see if the count exceeds 10 within a short time window.  
- Investigate the source IPs if they appear repeatedly or in other logs with suspicious behavior.  
- Obtain or define heuristic and taxonomy context to better classify future anomalies.