Based on the current log, a ping flooding attack is not happening.

Explanation:  
The Zeek conn.log entries show multiple ICMP connections with type 8 (Echo Request), which is consistent with ping traffic. However, the volume and timing do not indicate a flood. For example, the packets have normal sizes (56 bytes) and reasonable intervals (e.g., 0.008 seconds between packets), but there is no evidence of overwhelming traffic or rapid repeated requests from a single source to a single destination that would characterize a ping flood.

No anomaly records or heuristic context are provided to suggest otherwise. Without explicit heuristics or taxonomy, the presence of ICMP type 8 alone is insufficient to confirm a ping flood.

Next steps:  
- Monitor for a sudden surge in ICMP Echo Requests from one or multiple IPs targeting a single host or network segment.  
- Investigate any unusual spikes in ICMP traffic volume or rate.  
- Correlate with other logs or alerts for potential distributed denial-of-service (DDoS) activity.

If further data shows rapid, high-volume ICMP Echo Requests (type 8) overwhelming targets, then a ping flood attack should be confirmed and mitigated accordingly.